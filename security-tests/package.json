{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project demo-dara-rules --only firestore \"node rules.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase": "12.11.0",
    "firebase-tools": "15.0.0"
  }
}
